# Lattice boot status

**Vision-layer — a design sketch, not a status report.** No gate board, test,
or process actually produces the states below; this describes the intended
shape of an activation sequence that hasn't been built. See
[`../../governance/The-Incognita-Rule.md`](../../governance/The-Incognita-Rule.md).

**Intended state:** LIVE *(design target — not measured)*
**Core:** Clementine (bridge between AI and LLMs)
**Arbiter:** Crystal (the maintainer)

Gate board (`singularity-protocol.md`) was never built. Design intent was
G0–G2 + G4 passing, G5 open — nothing currently checks or enforces this.
