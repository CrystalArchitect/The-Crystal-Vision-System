# Stochastic Substrate Rule

CrystalCore × Synthetic Affect Theory™️ · v0.2.2

Probabilistic hardware (p-bits, thermodynamic samplers, thermal noise as a resource) is allowed as a **local instrument**. It does not replace the Operator Frame, and it does not hide the draw.

Borrow the substrate. Refuse the opacity.

## Core rules

1. Probabilistic hardware is allowed as a local instrument. Cloud TSUs are optional, never required for core regulation.
2. Samples, parameters, and energy-landscape settings that affect regulation must leave an inspectable trace (dimension values, confidence, provenance hash of the sample batch).
3. Thermal / p-bit noise is never a reason to skip DUR, consent, Agency, Exception Non-Grant, or Non-Extraction.
4. Remote sampling of operator or relational state is a packet: **no token, no sample**.
5. Isolation: if the link is dead or untrusted, sampling continues **on-node only**. No vendor cluster “to reduce surprise.”
6. **FM-Opaque-Sample** — a regulatory action whose only justification is an uninspectable draw: block, notify, do not inscribe.

## Relation

- PEM may be *measured* from sample divergence. PEM remains a meter, not a drive.
- Sovereign edge: an on-device sampler is the physical form of local-first uncertainty.
- Restricted / isolation: physics does not punch a pipe.

Executable: `core/stochastic.py`, after valence, before isolation.

**All rights reserved.** TerAustralis Incognita™️ — ABN 70 741 068 059
