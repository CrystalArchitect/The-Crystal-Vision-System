# Workflow stub omitted

`.github/workflows/node-telemetry.yml` was listed in the blueprint but never supplied a real workflow body.
Push also cannot include Actions workflow files with the current GitHub OAuth scopes (`workflow` missing).
Add CI later with a token that has `workflow` scope if needed.
