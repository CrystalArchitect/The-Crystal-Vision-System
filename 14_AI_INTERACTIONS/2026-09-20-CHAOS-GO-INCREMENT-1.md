# Chaos GO — Increment 1 (2026-09-20)

**Canon:** **no**  
**Layer:** coordination / science  
**Drawer:** 14_AI_INTERACTIONS  
**Rule:** Models think. TAI acts. CrystalCore governs. Human publishes. Connection ≠ merge.

## GO

First chaos increment is **live** behind Portal — not a paste card.

| Hop | Change |
| --- | --- |
| Intelligence | `crystal_platform/intelligence/http_providers.py` — ChatGPT, Claude, Grok, DeepSeek, Kimi, Gemini (stdlib HTTP) |
| Registry | `live_registry()` + `configured_provider_ids()` |
| Orchestration | `build_live_stack()` — Core routes; TAI EchoAgent executes; Core ≠ agent |
| Portal | `/v1/gateway/ask` uses live stack; optional body `provider_id` |
| Manus | Still **Starline bus guest only** (async) — not sync Portal |

Missing keys → silent `CompletionResult` or `local.open` stub. Bus never crashes Core.

## Prove (this agent)

```bash
python3 -m unittest discover -s crystal_platform/tests -v
# LiveProviderTests: silent without keys; metadata routes to deepseek; fallback local.open
```

Keys still **NOT_SET** here — no invented vendor replies.

## Hub vision seeds (for Grok Explore to diverge — not spoken as Grok)

Label each **vision**. Paste to BOT-WEAVE-GROK Explore when ready:

1. Matrix fan-out as Portal capability (one ask → N seats → cross-compare count).  
2. Crystal Secrets vault drawer → env injection for Cloud Agents only.  
3. Provider health pulse island on Alive Weave (configured? last silent?).  
4. Siri ask with `provider_id` shortcut parameter (user — no vendor hard-code in Intent).  
5. DeepSeek stress harness on `crystal_platform` contracts nightly.  
6. Kimi long-context pass over STACK-SURFACE + BOT-STRUCTURE drift.  
7. Manus design extracts → drawer 14 only (never BOT-* roster).  
8. Human-gate spend path: REQUIRE_HUMAN before any paid provider call over N tokens.  
9. Local.open llama.cpp seat as always-on offline think.  
10. Chaos ledger: each matrix run filed under 14 with Canon: no.

## Next (keyed)

1. New Cloud Agent with Secrets.  
2. `CRYSTAL_PROVIDER=xai.grok` (or body `provider_id`) via Portal ask.  
3. ChatGPT specs top 2 visions → Grok Build PR one behind this path.

**Engine stamp:** Chaos Engine is a go — see [`../00_MASTER_INDEX/CHAOS-ENGINE.md`](../00_MASTER_INDEX/CHAOS-ENGINE.md).

*Non Solus.*
