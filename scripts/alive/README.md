# Alive weave runners (hub coordination)

See [`00_MASTER_INDEX/ALIVE-WEAVE.md`](../00_MASTER_INDEX/ALIVE-WEAVE.md).

```bash
python3 scripts/alive/pulse.py
python3 scripts/alive/weave.py              # ConsentGate on by default
python3 scripts/alive/weave.py --sat
python3 scripts/alive/weave.py --no-gate
```
